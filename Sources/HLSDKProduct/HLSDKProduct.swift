import Foundation
import HLSDK

// This wrapper keeps third-party source dependencies in the package graph
// while exposing the HLSDK library product.
public enum HLSDKProduct {
    public static let version = "template"
}
